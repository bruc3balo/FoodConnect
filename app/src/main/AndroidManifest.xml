<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.victoria.foodconnect">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".utils.NotificationChannelClass"
        android:allowBackup="false"
        android:icon="@drawable/ic_give_food"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_give_food"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodConnect">
        <activity
            android:name=".pages.ProgressActivity"
            android:exported="false" />
        <activity
            android:name=".pages.transporter.donationProgress.DonationProgressActivity"
            android:exported="false" />
        <activity
            android:name=".pages.transporter.MoreDonationActivity"
            android:exported="false" />
        <activity
            android:name=".pages.donor.AddItemDonor"
            android:exported="false" />
        <activity
            android:name=".pages.transporter.jobProgress.JobActivityProgress"
            android:exported="false" />
        <activity
            android:name=".pages.LocationOrder"
            android:exported="false" />
        <activity
            android:name=".pages.transporter.MoreActivity"
            android:exported="false" />
        <activity
            android:name=".pages.CartActivity"
            android:exported="true" />
        <activity
            android:name=".pages.seller.ManageProduct"
            android:exported="true" />
        <activity
            android:name=".pages.donor.DonorProfile"
            android:exported="true" />
        <activity
            android:name=".pages.admin.ProductCategoryManagement"
            android:exported="true" />
        <activity
            android:name=".login.VerifyAccount"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".pages.seller.AddNewProduct"
            android:exported="true" />
        <activity android:name=".pages.admin.AdminActivity" />
        <activity android:name=".pages.beneficiary.BeneficiaryActivity" />
        <activity android:name=".pages.donor.DonorActivity" />
        <activity android:name=".pages.seller.SellerActivity" />
        <activity android:name=".pages.transporter.TransporterActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pages.beneficiary.fragment.OrdersFragment" />
        <activity android:name=".pages.beneficiary.fragment.ReceiveDonationsFragment" />

        <service android:name=".service.UploadPictureService" />
        <service
            android:name=".service.LocationService"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>